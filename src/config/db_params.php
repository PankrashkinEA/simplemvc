<?php

return array(
            'host' => 'db',
            'port' => 5432,
            'dbname' => 'barbotdb',
            'user' => 'barbot',
            'password' => 'secret',
);


            // $host = 'db';
            // $port = 5432;
            // $dbname = 'barbotdb';
            // $user = 'barbot';
            // $password = 'secret';
            // $dsn = "pgsql:host={$host};port={$port};dbname={$dbname}";
            // $db = new PDO($dsn, $user, $password);